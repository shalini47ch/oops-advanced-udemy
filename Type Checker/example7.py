def area(width:float,height:float)->float:
    return width*height 

print(area(10.00,10.00))

#to check for the types we will use a typechecker like mypy
#when we put the correct types it shows success that is no errors and in other cases it shows errors of the types for the width and height
